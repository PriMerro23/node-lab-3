<!DOCTYPE html>
<html lang="uk">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lotify</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
    <header>
        <a href="/" class="logo">Lotify</a>
        <div class="search-bar">
            <input type="search" placeholder="Пошук..." />
            <button>Пошук</button>
        </div>
        <div class="profile">
            <a href="/users/profile">Профіль</a>
        </div>
    </header>

    <main>
        <div class="lots">
            <% if (lots && lots.length> 0) { %> <% lots.forEach(function(lot) { %>
                    <div class="lot">
                        <img src="<%= lot.image ? lot.image : '/images/placeholder.jpg' %>" alt="<%= lot.title %>" />
                        <h3>
                            <%= lot.title %>
                        </h3>
                        <p class="desc">
                            <%= lot.description %>
                        </p>
                        <div class="price">
                            <p>Початкова ціна: <%= lot.start_price %>
                            </p>
                            <p>Ціна: <%= lot.current_price %>
                            </p>
                        </div>

                        <a class="more" href="/lots/<%= lot.id %>">Дізнатись більше</a>
                        <!-- <a class="offer" href="/offers/?lotId=<%= lot.id %>">Your Offer</a> -->
                    </div>
                    <% }); %>
                        <% } else { %>
                            <p>На даний момент немає доступних лотів.</p>
                            <% } %>
        </div>
    </main>
</body>

</html>